local options = {
  ensure_installed = { "c", "go", "rust", "lua", "vim", "vimdoc", "query", "typescript", "javascript", "python"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
